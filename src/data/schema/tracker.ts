import { z } from "zod";
const trackerSchema = [
	{
		name: "accountName",
		valueType: "string",
		required: true,
		nullable: false,
		zod: z.string().min(2),
		zodError: "Account name is required and at least 2 characters",
		creatable: true,
		editable: true,
		defaultValue: "",
	},
	{
		name: "service",
		valueType: [
			"Instagram",
			"Twitter",
			"Facebook",
			"TikTok",
			"Snapchat",
			"LinkedIn",
		],
		required: true,
		nullable: false,
		zod: z.enum([
			"Instagram",
			"Twitter",
			"Facebook",
			"TikTok",
			"Snapchat",
			"LinkedIn",
		]),
		zodError: "Invalid selected value",
		creatable: true,
		editable: true,
		defaultValue: "Instagram",
	},
	{
		name: "lastAccessed",
		valueType: "number",
		required: true,
		nullable: true,
		zod: z.number().nullable(),
		zodError: "Value must be either null or a number",
		creatable: false,
		editable: false,
	},
	{
		name: "lastAccessedId",
		valueType: "string",
		required: true,
		nullable: true,
		zod: z.string().nullable(),
		zodError: "Value must be either null or a string",
		creatable: false,
		editable: false,
	},
	{
		name: "status",
		valueType: ["active", "inactive"],
		required: true,
		nullable: false,
		zod: z.enum(["active", "inactive"]),
		zodError: "Invalid selected value",
		creatable: true,
		editable: true,
		defaultValue: "active",
	},
];

export default trackerSchema;
